import _ from 'lodash'
import { faker } from '@faker-js/faker'

import { SocialSeeder, Seeder, seedBanks } from '../utils/DocSeeders'

export default defineEventHandler(async (e) => {
  // Seeder.seed()
  // seedBanks()
  // SocialSeeder.seed()
  return {
    posts: [],
  }
})
